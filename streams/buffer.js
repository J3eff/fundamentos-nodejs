// buffer -> Representa um espaço na memória onde podemos armazenar dados
// streams -> Permite trabalhar com fluxos de dados (leitura e escrita de forma contínua)

const buf = Buffer.from('ok');

console.log(buf.toJSON());